include:
  - path: ./compose/compose.platform-admin.yml
    project_directory: .

services:
  redis:
    image: redis:7.2.3-alpine3.18
    ports:
      - '6379:6379'
    networks:
      - cdp-tenant

networks:
  cdp-tenant:
    driver: bridge
