{% extends 'layouts/page.njk' %}

{% from "application-header/macro.njk" import appApplicationHeader %}
{% from "available-entitlements/macro.njk" import appAvailableEntitlements %}
{% from "govuk/components/service-navigation/macro.njk" import govukServiceNavigation %}
{% from "govuk/components/details/macro.njk" import govukDetails %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block content %}
  {{ appApplicationHeader(header) }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">

      {{ govukServiceNavigation({
        navigation: tabs,
        navigationLabel: "Application sections",
        classes: "app-application-tabs govuk-!-margin-top-5"
      }) }}

      <div class="app-section-panel">
        {{ appAvailableEntitlements({
          entitlements: entitlements,
          creatingClaimCode: claimableTemplate.claimCode
        }) }}

        <div class="app-card" id="new-entitlement">
          <h2 class="govuk-heading-m" data-testid="claimable-heading">Add claimable item</h2>

          {% if claimableTemplate.description %}
            <p class="govuk-body" data-testid="claimable-template-description">
              {{ claimableTemplate.description }}
            </p>
          {% endif %}

          <form method="post" novalidate>
            {% for key, field in claimableTemplate.fields %}
              {% if field.input %}
                {% set unitType = field.unitType | lower %}
                {% if unitType === "decimal" %}
                  {% set inputmode = "decimal" %}
                {% elif unitType === "integer" %}
                  {% set inputmode = "numeric" %}
                {% else %}
                  {% set inputmode = none %}
                {% endif %}

                {{ govukInput({
                  id: key,
                  name: key,
                  label: { text: field.label, classes: "govuk-label--s" },
                  classes: "govuk-input--width-10",
                  inputmode: inputmode,
                  spellcheck: false if inputmode else none,
                  suffix: { text: field.unit | lower } if field.unit else none,
                  attributes: { "data-testid": "claimable-field-" + key }
                }) }}
              {% endif %}
            {% endfor %}

            {% if claimableTemplate.help %}
              {% set helpHtml %}
                {% for block in claimableTemplate.help.content %}
                  {% if block.items %}
                    <ul class="govuk-list govuk-list--bullet">
                      {% for item in block.items %}
                        <li>{{ item }}</li>
                      {% endfor %}
                    </ul>
                  {% else %}
                    <p class="govuk-body">{{ block.text }}</p>
                  {% endif %}
                {% endfor %}
              {% endset %}

              {{ govukDetails({
                summaryText: claimableTemplate.help.summary,
                html: helpHtml,
                attributes: { "data-testid": "claimable-help" }
              }) }}
            {% endif %}

            <div class="govuk-button-group">
              {{ govukButton({
                text: "Save item",
                attributes: { "data-testid": "claimable-submit" }
              }) }}

              <a class="govuk-link" href="{{ claimsHref }}" data-testid="claimable-cancel">Cancel</a>
            </div>
          </form>
        </div>
      </div>

    </div>
  </div>
{% endblock %}
