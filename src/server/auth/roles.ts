/**
 * Entra ID app roles, as assigned on the application registration.
 * These arrive on the `roles` claim and become the credentials scope.
 */
export const roles = {
  grantApplicationsAdmin: 'FCP.GrantApplicationsAdmin',
  grantOperationsAdmin: 'FCP.GrantOperationsAdmin'
}
