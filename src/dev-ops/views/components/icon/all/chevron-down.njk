{# lucide (ISC), `chevron-down`, copied verbatim from https://lucide.dev — the
   root element is ours, carrying the caller's classes. The one mark that says
   a button opens something rather than doing something. #}
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"{% if params.class %} class="{{ params.class }}"{% endif %} data-testid="do-icon-chevron-down">
  <path d="m6 9 6 6 6-6"/>
</svg>
