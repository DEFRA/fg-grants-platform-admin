{# From the design handoff's alert (tickets/FGP-1392 · Event Stream.dc.html),
   copied verbatim: a stroked 24-grid triangle, not the filled heroicon, so the
   two alert icons read as one pair at the 16px the alert sets them in. #}
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" aria-hidden="true"{% if params.class %} class="{{ params.class }}"{% endif %} data-testid="do-icon-exclamation-triangle">
  <path d="M12 9v5M12 17h.01M10.3 4.3 2.6 18a1.6 1.6 0 0 0 1.4 2.4h16a1.6 1.6 0 0 0 1.4-2.4L13.7 4.3a1.6 1.6 0 0 0-2.8 0z"/>
</svg>
