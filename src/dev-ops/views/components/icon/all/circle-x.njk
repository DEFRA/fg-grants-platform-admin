{# lucide (ISC), `circle-x`, copied verbatim from https://lucide.dev — the root
   element is ours, carrying the caller's classes. One per rung of the attempts
   timeline: every attempt on that list is an attempt that failed. #}
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"{% if params.class %} class="{{ params.class }}"{% endif %} data-testid="do-icon-circle-x">
  <circle cx="12" cy="12" r="10"/>
  <path d="m15 9-6 6"/>
  <path d="m9 9 6 6"/>
</svg>
