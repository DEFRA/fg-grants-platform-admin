{# lucide (ISC), `search`, copied verbatim from https://lucide.dev — the root
   element is ours, carrying the caller's classes. Sits inside the search
   `label.input`, where daisyUI expects a leading glyph. #}
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"{% if params.class %} class="{{ params.class }}"{% endif %} data-testid="do-icon-search">
  <circle cx="11" cy="11" r="8"/>
  <path d="m21 21-4.3-4.3"/>
</svg>
